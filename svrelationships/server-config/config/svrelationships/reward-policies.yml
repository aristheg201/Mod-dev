default:
  offline_policy: current_only
  max_pending_periods: 1
  overflow_policy: drop

profiles:
  partner_daily:
    offline_policy: current_only
    max_pending_periods: 1
    overflow_policy: drop
