base { archivesName = 'MythicLib-Fabric' }

// MythicLib is fully materialized in this module. Do not generate or overlay
// production Java sources at build time: the canonical source tree is the
// single source of truth compiled by Loom.
