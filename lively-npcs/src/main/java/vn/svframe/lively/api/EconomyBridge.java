package vn.svframe.lively.api;
import java.math.BigDecimal;import java.util.UUID;public interface EconomyBridge{boolean available();BigDecimal balance(UUID actor,String currency);void deposit(UUID actor,BigDecimal amount,String currency);boolean withdraw(UUID actor,BigDecimal amount,String currency);boolean transfer(UUID from,UUID to,BigDecimal amount,String currency);static EconomyBridge unavailable(){return new EconomyBridge(){public boolean available(){return false;}public BigDecimal balance(UUID actor,String currency){return BigDecimal.ZERO;}public void deposit(UUID actor,BigDecimal amount,String currency){}public boolean withdraw(UUID actor,BigDecimal amount,String currency){return false;}public boolean transfer(UUID from,UUID to,BigDecimal amount,String currency){return false;}};}}
