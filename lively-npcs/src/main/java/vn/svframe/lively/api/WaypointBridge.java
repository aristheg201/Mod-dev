package vn.svframe.lively.api;
import net.minecraft.server.network.ServerPlayerEntity;import net.minecraft.util.math.Vec3d;public interface WaypointBridge{boolean available();boolean show(ServerPlayerEntity player,String key,String world,Vec3d target,String label);boolean clear(ServerPlayerEntity player,String key);static WaypointBridge unavailable(){return new WaypointBridge(){public boolean available(){return false;}public boolean show(ServerPlayerEntity player,String key,String world,Vec3d target,String label){return false;}public boolean clear(ServerPlayerEntity player,String key){return false;}};}}
